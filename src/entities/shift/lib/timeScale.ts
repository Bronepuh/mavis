/**
 * Количество миллисекунд в одном часе.
 * Используется для расчёта длительности смен и перевода часов → мс.
 */
export const HOUR_MS = 60 * 60 * 1000;

/**
 * Размер одного деления временной сетки на таймлайне (в часах).
 * Влияет на шаг меток и вычисление ширины колонки.
 */
export const COL_HOURS = 2;
